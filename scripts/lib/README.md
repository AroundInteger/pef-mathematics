# MATLAB libraries in this repo

## Companion-owned (edit here)

| File | Role |
|------|------|
| `pef_geometry_helpers.m` | Half-strip geometry primitives for `run_pef_geometry_numerical_model.m` (canonical $\eta$, sphere lift, signed $\psi$, geometric-family sampler, BVN-on-$\tau$ sampler) |

## Mirrored from empirical (do not edit)

> Auto-generated by `sync_to_companion.sh` in
> [pef-empirical](https://github.com/AroundInteger/pef-empirical).
>
> **Do not edit** `pef_theory_helpers.m` here. Edit the authoritative copy in
> `pef-empirical/scripts/paper_pipeline/lib/` and re-run
> `bash scripts/paper_pipeline/sync_to_companion.sh`.

The mirror exists so reviewers of the companion §7 empirical CSV tests can
inspect the exact MATLAB primitives (`eta_pef`, `classify_quadrant`,
`mi_closed`, ...) that produced `validation_inputs/*.csv` without needing
to clone the empirical repo.
